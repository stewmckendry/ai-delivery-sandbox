folders:
  base_path: "PolicyGPT"
  structure:
    - key: project  # NOTE: Not yet supported in schema — to be added in future
      source: metadata
      field: project_id
    - key: gate
      source: metadata
      field: gate_id
    - key: artifact
      source: metadata
      field: artifact_id
  filename_pattern: "{artifact_id}_v{version}_{timestamp}.pdf"
  allow_overwrite: false
  timestamp_format: "%Y%m%dT%H%M%S"